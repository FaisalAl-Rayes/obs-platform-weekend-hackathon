apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: tower-gitea-creds
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault
    kind: ClusterSecretStore
  target:
    name: tower-gitea-creds
  data:
    - secretKey: username
      remoteRef:
        key: platform/tower
        property: username
    - secretKey: password
      remoteRef:
        key: platform/tower
        property: password
