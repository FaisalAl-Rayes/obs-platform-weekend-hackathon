apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ria-prod

resources:
  - ../../base

patches:
  - target:
      kind: Namespace
      name: ria
    patch: |
      - op: replace
        path: /metadata/name
        value: ria-prod
