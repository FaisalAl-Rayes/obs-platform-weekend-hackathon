apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ria-stage

resources:
  - ../../base

patches:
  - target:
      kind: Namespace
      name: ria
    patch: |
      - op: replace
        path: /metadata/name
        value: ria-stage
